#include <iostream>

using namespace std;
int main() {

	string destination, dates; cin >> destination >> dates;
	int nights; cin >> nights;
	int priceNight = 0;
	double totalExp = 0;

	if (destination == "France")
	{
		if (dates == "21-23")
		{
			priceNight = 30;
		}
		else if (dates == "24-27")
		{
			priceNight = 35;
		}
		else if (dates == "28-31")
		{
			priceNight = 40;
		}
	}
	else if (destination == "Italy")
	{
		if (dates == "21-23")
		{
			priceNight = 28;
		}
		else if (dates == "24-27")
		{
			priceNight = 32;
		}
		else if (dates == "28-31")
		{
			priceNight = 39;
		}
	}
	else if (destination == "Germany")
	{
		if (dates == "21-23")
		{
			priceNight = 32;
		}
		else if (dates == "24-27")
		{
			priceNight = 37;
		}
		else if (dates == "28-31")
		{
			priceNight = 43;
		}
	}
	cout.setf(ios::fixed);
	cout.precision(2);
	totalExp = priceNight * 1.0 * nights;

	cout << "Easter trip to " << destination << " : " << totalExp << " leva.";

	return 0;
}