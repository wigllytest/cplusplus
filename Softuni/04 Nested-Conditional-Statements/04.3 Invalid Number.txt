#include <iostream>

using namespace std;

int main() {

	int num;
	cin >> num;

	//if (((num >= 100) && (num <= 200) || num == 0 ))
	//{
	//}
	//	else
	//	{
	//		cout << "Invalid";
	//	}
	
	bool isValid = (num >= 100) && (num <= 200) || num == 0;
	if (!isValid) {
		cout << "Invalid" << endl;
	}

	
	return 0;
}